# 🔄 ORA Platform - Complete Workflow Diagram

Visual guide to understand how everything connects.

---

## 🎯 High-Level Flow

```
┌──────────────┐
│   You Code   │
│ (VS Code)    │
└──────┬───────┘
       │
       │ git push
       ▼
┌──────────────┐
│   GitHub     │ ◄─── Version Control
│ (Repository) │      (Your Code History)
└──────┬───────┘
       │
       │ Auto-Deploy (GitHub Actions)
       │ or Manual Deploy
       ▼
┌──────────────────────────────────────────┐
│         PRODUCTION HOSTING               │
│    (Vercel / Firebase / Netlify)         │
│                                           │
│  ┌─────────────────────────────────┐    │
│  │  Your React App (Built)         │    │
│  │  • HTML/CSS/JS files            │    │
│  │  • Served to users              │    │
│  └─────────────────────────────────┘    │
└───────────────┬──────────────────────────┘
                │
                │ API Calls
                ▼
┌──────────────────────────────────────────┐
│            SUPABASE                      │
│                                           │
│  ┌────────────────────────────────┐     │
│  │   Edge Functions (API)         │     │
│  │   /make-server-3504d096        │     │
│  └──┬─────────────────────────┬───┘     │
│     │                         │          │
│     │ Read/Write              │ Upload   │
│     ▼                         ▼          │
│  ┌─────────────┐    ┌──────────────┐   │
│  │  Database   │    │   Storage    │   │
│  │  (KV Store) │    │  (PDF Files) │   │
│  └─────────────┘    └──────────────┘   │
└──────────────────────────────────────────┘
```

---

## 📱 User Journey - Uploading a PDF

```
┌──────────────────────────────────────────────────────────────┐
│ STEP 1: User Opens PDF Library                              │
└──┬───────────────────────────────────────────────────────────┘
   │
   │ 1. Click "Library" in navigation
   │
   ▼
┌──────────────────────────────────────────────────────────────┐
│ FRONTEND: PDFLibrary Component Loads                        │
│                                                               │
│ • Fetch existing PDFs                                        │
│ • GET /pdf/list                                              │
└──┬───────────────────────────────────────────────────────────┘
   │
   │ HTTP Request
   │
   ▼
┌──────────────────────────────────────────────────────────────┐
│ BACKEND: Supabase Edge Function                             │
│                                                               │
│ • Receives request                                           │
│ • Queries KV store for all pdf: keys                        │
│ • Returns array of PDF metadata                             │
└──┬───────────────────────────────────────────────────────────┘
   │
   │ Response (JSON)
   │
   ▼
┌──────────────────────────────────────────────────────────────┐
│ FRONTEND: Display PDFs in Grid/List                         │
│                                                               │
│ • User clicks "Upload PDF"                                   │
│ • Modal opens                                                │
└──┬───────────────────────────────────────────────────────────┘
   │
   │ 2. User fills form and selects file
   │
   ▼
┌──────────────────────────────────────────────────────────────┐
│ STEP 2: User Submits Upload Form                            │
│                                                               │
│ • File selected                                              │
│ • Title, description, category, tags entered                │
│ • Click "Upload"                                             │
└──┬───────────────────────────────────────────────────────────┘
   │
   │ FormData with file
   │
   ▼
┌──────────────────────────────────────────────────────────────┐
│ BACKEND: POST /pdf/upload                                   │
│                                                               │
│ Step 1: Validate input                                       │
│ Step 2: Generate unique filename                            │
│ Step 3: Upload to Supabase Storage bucket                  │
│         • Bucket: make-3504d096-pdfs                        │
│         • Private bucket                                     │
│ Step 4: Generate signed URL (1 year validity)              │
│ Step 5: Save metadata to KV store                          │
│         • Key: pdf:{uuid}                                    │
│         • Value: {title, url, category, tags...}           │
│ Step 6: Return success response                            │
└──┬───────────────────────────────────────────────────────────┘
   │
   │ Success response
   │
   ▼
┌──────────────────────────────────────────────────────────────┐
│ FRONTEND: Show Success & Refresh List                       │
│                                                               │
│ • Close modal                                                │
│ • Fetch PDFs again                                           │
│ • Display new PDF in grid                                    │
└──────────────────────────────────────────────────────────────┘
   │
   │ 3. User clicks download
   │
   ▼
┌──────────────────────────────────────────────────────────────┐
│ STEP 3: Download PDF                                        │
│                                                               │
│ • Use signed URL from metadata                               │
│ • Browser downloads from Supabase Storage                   │
│ • No additional API call needed                              │
└──────────────────────────────────────────────────────────────┘
```

---

## 🔄 Development to Production Flow

```
┌─────────────────────────────────────────────────────────────┐
│                   DEVELOPMENT PHASE                         │
└─────────────────────────────────────────────────────────────┘

1. CODE IN VS CODE
   ├── Edit React components
   ├── Modify backend functions
   ├── Update styling
   └── Test locally (npm run dev)
           │
           │ Save changes
           ▼
2. LOCAL TESTING
   ├── http://localhost:5173
   ├── Check browser console
   ├── Test all features
   └── Verify no errors
           │
           │ git add, commit
           ▼
3. VERSION CONTROL
   ├── git add .
   ├── git commit -m "Added PDF library"
   ├── git push origin main
   └── Code on GitHub
           │
           │ Trigger deployment
           ▼

┌─────────────────────────────────────────────────────────────┐
│                  DEPLOYMENT PHASE                           │
└─────────────────────────────────────────────────────────────┘

4. BUILD PROCESS
   ├── npm run build
   ├── Vite compiles React → static files
   ├── Tailwind processes CSS
   └── Output: /dist folder
           │
           │ Upload to hosting
           ▼
5. HOSTING PLATFORM
   ├── Vercel/Firebase/Netlify receives build
   ├── Serves static files via CDN
   ├── Connects to environment variables
   └── Live at production URL
           │
           │ Users visit
           ▼

┌─────────────────────────────────────────────────────────────┐
│                   PRODUCTION PHASE                          │
└─────────────────────────────────────────────────────────────┘

6. USER INTERACTION
   ├── Browser loads React app
   ├── JavaScript executes
   ├── Makes API calls to Supabase
   └── Data flows between frontend and backend
           │
           │ Continuous monitoring
           ▼
7. MONITORING & UPDATES
   ├── Check logs in Supabase
   ├── Monitor errors
   ├── Make improvements
   └── Push updates (repeat cycle)
```

---

## 🏗️ System Architecture Layers

```
┌────────────────────────────────────────────────────────────┐
│  LAYER 1: PRESENTATION (What Users See)                   │
├────────────────────────────────────────────────────────────┤
│                                                             │
│  React Components                                          │
│  ├── Navigation.tsx          ┐                            │
│  ├── PDFLibrary.tsx          │ UI Components              │
│  ├── AIAgent.tsx             │                            │
│  ├── LessonContent.tsx       │                            │
│  └── ...30+ more             ┘                            │
│                                                             │
│  Styling: Tailwind CSS + Custom Theme                     │
│  State: React Hooks (useState, useEffect)                 │
│                                                             │
└─────────────────┬──────────────────────────────────────────┘
                  │
                  │ API Calls (fetch)
                  ▼
┌────────────────────────────────────────────────────────────┐
│  LAYER 2: API / BACKEND (Business Logic)                  │
├────────────────────────────────────────────────────────────┤
│                                                             │
│  Supabase Edge Functions (Deno + Hono)                    │
│  /supabase/functions/server/index.tsx                     │
│                                                             │
│  Endpoints:                                                │
│  ├── GET  /health              ─ Health check             │
│  ├── POST /pdf/upload          ─ Upload PDF               │
│  ├── GET  /pdf/list            ─ List all PDFs            │
│  ├── GET  /pdf/:id             ─ Get single PDF           │
│  ├── PUT  /pdf/:id             ─ Update metadata          │
│  └── DELETE /pdf/:id           ─ Delete PDF               │
│                                                             │
│  Authentication: Bearer token                              │
│  CORS: Enabled for all origins                            │
│                                                             │
└─────────────────┬──────────────────────────────────────────┘
                  │
                  │ Data Operations
                  ▼
┌────────────────────────────────────────────────────────────┐
│  LAYER 3: DATA (Persistence)                              │
├────────────────────────────────────────────────────────────┤
│                                                             │
│  A. Supabase PostgreSQL Database                          │
│     └── kv_store_3504d096 table                           │
│         ├── Key: pdf:{uuid}                                │
│         └── Value: {metadata as JSON}                      │
│                                                             │
│  B. Supabase Storage (S3-compatible)                      │
│     └── make-3504d096-pdfs bucket                         │
│         ├── Private bucket                                 │
│         ├── PDF files stored                               │
│         └── Signed URLs for access                         │
│                                                             │
│  C. Browser LocalStorage (Client-side)                    │
│     ├── theme (dark/light)                                 │
│     ├── userSentiments (array)                             │
│     └── lastSentimentCheckIn (timestamp)                   │
│                                                             │
└────────────────────────────────────────────────────────────┘
```

---

## 🔐 Authentication Flow (Google OAuth)

```
┌──────────────────────────────────────────────────────────────┐
│ STEP 1: User Clicks "Sign in with Google"                  │
└──┬───────────────────────────────────────────────────────────┘
   │
   ▼
┌──────────────────────────────────────────────────────────────┐
│ STEP 2: Firebase Auth Initiated                            │
│                                                               │
│ • Opens Google OAuth popup                                   │
│ • User selects Google account                               │
│ • User grants permissions                                    │
└──┬───────────────────────────────────────────────────────────┘
   │
   │ Redirect to Google
   │
   ▼
┌──────────────────────────────────────────────────────────────┐
│ STEP 3: Google Authentication                               │
│                                                               │
│ • Google verifies credentials                                │
│ • Checks authorized domains                                  │
│ • Validates OAuth client ID                                  │
└──┬───────────────────────────────────────────────────────────┘
   │
   │ Redirect back
   │
   ▼
┌──────────────────────────────────────────────────────────────┐
│ STEP 4: Firebase Receives Auth Code                         │
│                                                               │
│ • Exchanges code for tokens                                  │
│ • Creates user session                                       │
│ • Returns access token                                       │
└──┬───────────────────────────────────────────────────────────┘
   │
   │ User session created
   │
   ▼
┌──────────────────────────────────────────────────────────────┐
│ STEP 5: Frontend Stores Session                             │
│                                                               │
│ • Save access token                                          │
│ • Store user info                                            │
│ • Update UI (show user profile)                             │
└──┬───────────────────────────────────────────────────────────┘
   │
   │ Subsequent requests
   │
   ▼
┌──────────────────────────────────────────────────────────────┐
│ STEP 6: Authenticated API Calls                             │
│                                                               │
│ • Include access token in Authorization header              │
│ • Backend verifies token                                     │
│ • Access granted to protected resources                      │
└──────────────────────────────────────────────────────────────┘
```

---

## 🚀 Continuous Deployment Pipeline

```
┌──────────────────────────────────────────────────────────────┐
│                    DEVELOPER WORKFLOW                        │
└──────────────────────────────────────────────────────────────┘

1. Write code in VS Code
       │
       │ Save files
       ▼
2. Test locally
   npm run dev
       │
       │ Everything works
       ▼
3. Commit changes
   git add .
   git commit -m "Added feature X"
       │
       │ Push to GitHub
       ▼
4. Push to GitHub
   git push origin main
       │
       │ Triggers GitHub Actions
       ▼

┌──────────────────────────────────────────────────────────────┐
│              AUTOMATED CI/CD PIPELINE                        │
│                 (GitHub Actions)                             │
└──────────────────────────────────────────────────────────────┘

5. GitHub Actions Triggered
       │
       │ Run workflow
       ▼
6. Install Dependencies
   npm ci
       │
       ▼
7. Run Tests (if configured)
   npm test
       │
       ▼
8. Build Application
   npm run build
       │
       │ Inject environment variables
       ▼
9. Deploy to Hosting
   Vercel/Firebase/Netlify
       │
       │ Upload build files
       ▼
10. Deployment Complete
    ✅ Live on production URL
       │
       │ Notify via email/Slack
       ▼
11. Monitor & Verify
    • Check deployment status
    • Run smoke tests
    • Monitor errors

┌──────────────────────────────────────────────────────────────┐
│                    RESULT                                    │
│  Your changes are now live in production!                   │
│  Typically takes: 2-5 minutes from push to live             │
└──────────────────────────────────────────────────────────────┘
```

---

## 📊 Data Flow Example: Viewing PDFs

```
┌─────────────┐
│    USER     │
│  (Browser)  │
└──────┬──────┘
       │
       │ 1. Navigate to /Library
       ▼
┌─────────────────────────────┐
│  App.tsx                    │
│  currentPage = "pdflibrary" │
└──────┬──────────────────────┘
       │
       │ 2. Render PDFLibrary component
       ▼
┌─────────────────────────────────────┐
│  PDFLibrary.tsx                     │
│  useEffect(() => fetchPDFs())       │
└──────┬──────────────────────────────┘
       │
       │ 3. API Call
       │ GET /pdf/list
       ▼
┌────────────────────────────────────────────────┐
│  Supabase Edge Function                       │
│  /make-server-3504d096/pdf/list               │
│                                                 │
│  const pdfs = await kv.getByPrefix('pdf:')    │
└──────┬─────────────────────────────────────────┘
       │
       │ 4. Database Query
       ▼
┌────────────────────────────────────────┐
│  Supabase PostgreSQL                   │
│  kv_store_3504d096 table               │
│                                         │
│  SELECT * FROM kv_store_3504d096       │
│  WHERE key LIKE 'pdf:%'                │
└──────┬─────────────────────────────────┘
       │
       │ 5. Return results
       │ [{id, title, url, ...}, ...]
       ▼
┌────────────────────────────────────────┐
│  Edge Function                         │
│  Sort by date, format response         │
└──────┬─────────────────────────────────┘
       │
       │ 6. Response (JSON)
       │ {pdfs: [...]}
       ▼
┌────────────────────────────────────────┐
│  PDFLibrary.tsx                        │
│  setPdfs(data.pdfs)                    │
└──────┬─────────────────────────────────┘
       │
       │ 7. React renders
       ▼
┌────────────────────────────────────────┐
│  Browser displays PDF grid             │
│  • Each card shows title, category    │
│  • Click to download/view             │
│  • Search and filter available        │
└────────────────────────────────────────┘
```

---

## 🌐 Multi-Environment Setup

```
┌──────────────────────────────────────────────────────────────┐
│                    DEVELOPMENT                               │
├──────────────────────────────────────────────────────────────┤
│  Location: localhost:5173                                    │
│  Environment: .env.local                                     │
│  Database: Supabase development project                     │
│  Purpose: Testing new features                              │
└──────────────────────────────────────────────────────────────┘
                           │
                           │ git push to 'staging' branch
                           ▼
┌──────────────────────────────────────────────────────────────┐
│                    STAGING (Optional)                        │
├──────────────────────────────────────────────────────────────┤
│  Location: staging.yourapp.com                              │
│  Environment: Production secrets (staging project)          │
│  Database: Supabase staging project                         │
│  Purpose: Pre-production testing                            │
└──────────────────────────────────────────────────────────────┘
                           │
                           │ git push to 'main' branch
                           ▼
┌──────────────────────────────────────────────────────────────┐
│                    PRODUCTION                                │
├──────────────────────────────────────────────────────────────┤
│  Location: yourapp.com                                       │
│  Environment: Production secrets                            │
│  Database: Supabase production project                      │
│  Purpose: Live users                                         │
└──────────────────────────────────────────────────────────────┘
```

---

## 🔧 Environment Variables Flow

```
┌──────────────────┐
│  .env (Local)    │  ◄─── Never commit to Git
│  Your secrets    │       Listed in .gitignore
└────────┬─────────┘
         │
         │ Read by Vite during build
         │
         ▼
┌──────────────────────────────────┐
│  Frontend Code                   │
│  import.meta.env.VITE_*          │
│                                   │
│  ✅ VITE_SUPABASE_URL            │
│  ✅ VITE_SUPABASE_ANON_KEY       │
│  ❌ SUPABASE_SERVICE_ROLE_KEY    │ ◄─── Backend only!
└──────────┬───────────────────────┘
           │
           │ Build process
           │
           ▼
┌──────────────────────────────────┐
│  Built App (dist/)               │
│  Variables are compiled in       │
│  (replaced at build time)        │
└──────────┬───────────────────────┘
           │
           │ Deploy
           │
           ▼
┌──────────────────────────────────┐
│  Production                      │
│  Uses variables from hosting     │
│  platform dashboard              │
│  (Vercel/Firebase/Netlify)       │
└──────────────────────────────────┘
```

---

## 📱 Mobile vs Desktop Flow

```
MOBILE DEVICE
├── Smaller viewport (< 768px)
├── Touch interactions
├── Mobile menu (hamburger)
├── Vertical scrolling optimized
└── Grid → 1 column

TABLET
├── Medium viewport (768px - 1024px)
├── Touch or mouse
├── Responsive layout
├── Grid → 2 columns
└── Some desktop features

DESKTOP
├── Large viewport (> 1024px)
├── Mouse/keyboard
├── Full navigation bar
├── Grid → 3 columns
└── All features visible
```

---

This workflow diagram should help visualize how all the pieces fit together!

For step-by-step deployment instructions, see:
- **QUICKSTART.md** - 30-minute setup
- **DEPLOYMENT_GUIDE.md** - Complete guide
